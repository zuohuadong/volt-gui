{
  "name": "voltui-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/fetch-community.mjs && node scripts/fetch-publications.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "test": "npm run build && node ../scripts/site-feature-smoke.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/inter": "^5.1.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@fontsource-variable/outfit": "^5.3.0",
    "astro": "^7.2.1"
  },
  "allowScripts": {
    "esbuild": true,
    "fsevents": true,
    "sharp": true
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  },
  "overrides": {
    "esbuild": "0.28.1"
  }
}
