# 西谷智灯暗涌系统

## 产品定位

西谷智灯暗涌系统是面向中国开发团队的本地桌面编码 Agent，运行时基于
Node 26、Electron 与官方 DeepSeek Harness。

## 架构原则

1. 官方 `@deepseek-ai/dsh` 是唯一 Harness 和产品 UI。
2. DSH 负责会话、工具、权限、凭据、工作区和持久化。
3. Electron 只负责窗口、安全、loopback 导航和 DSH 子进程生命周期。
4. `profiles/anyong.yml` 只通过官方 profile patch 覆盖模型默认值。
5. 仓库不维护本地 renderer、preload、私有 Harness 包或第二套状态层。
6. 外部源码不自动同步；依赖升级通过 npm 精确版本和独立 PR 评审。

## 发布边界

- CNB 使用 Node 26 和冻结锁文件执行测试、审计与构建。
- GitHub Windows runner 生成 Windows x64 Electron 安装包和 portable 产物。
- 正式签名、自动更新、公开发布、macOS 和 Linux 发布在合同完善前保持关闭。

## 品牌边界

- Electron 产品身份由 `apps/desktop-electron/src/electron-profile.ts` 管理。
- DSH 能力与默认模型通过官方 profile/plugin 扩展点配置。
- 不修改已安装的官方依赖，不复制官方 Web UI，不建立第二套品牌运行时。

## 验收标准

- `pnpm test`
- `pnpm run test:dsh-integration`
- `pnpm run build`
- `node scripts/check-migration-boundary.mjs`
- `cd site && npm ci && npm test`
- `git diff --check`
